# ADR-0005: Динамическое обнаружение

**Статус:** принято

Набор протоколов, ports, SNI, transports и Host rows не зашивается в код. Каждый
audit перечитывает текущую topology LucX. Неизвестный inbound инвентаризируется,
но не получает опасный маршрут по умолчанию.
