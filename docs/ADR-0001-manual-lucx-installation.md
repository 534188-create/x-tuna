# ADR-0001: Ручная установка LucX

**Статус:** принято

LucX имеет собственный installer, updater и версионную схему базы. Поэтому
`x-tuna` запускается после ручной установки LucX, создания inbound’ов и
клиентов. Конфигуратор обнаруживает текущую версию и останавливается при
несовместимости.
